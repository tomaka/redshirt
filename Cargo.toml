[workspace]
members = [
    "core",
    "kernel/cli",
    "kernel/hosted-tap",
    "kernel/hosted-stdout",
    "kernel/hosted-time",
    "kernel/standalone",
    "interfaces/hardware",
    "interfaces/interface",
    "interfaces/loader",
    "interfaces/network",
    "interfaces/pci",
    "interfaces/random",
    "interfaces/stdout",
    "interfaces/syscalls",
    "interfaces/threads",
    "interfaces/time",
    "interfaces/vulkan",
    "interfaces/window",
]

[profile.dev]
opt-level = 1

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = 'abort'
