pull_request_rules:
  - name: automatic merge when GitHub branch protection passes on develop
    conditions:
      - check-success=all-ci
      - label=automerge
      - base=develop
    actions:
      merge:
        strict: true
        method: squash
